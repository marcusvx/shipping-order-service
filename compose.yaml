services:
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: shipments@pass
      POSTGRES_USER: app_shipments_user
      POSTGRES_DB: app_shipments_db
    ports:
      - "5432:5432"  